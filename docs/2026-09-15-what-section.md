# Секция «Что делаем» на главной — спека переноса (2026-09-15)

Источник: `final-emil.html` (панель: Якуб Крехел → Эмиль Ковальски), принят Артёмом 15.09.2026.

## Что меняется в `index.html`
- Верх старой секции `#prod` (шапка «Два направления работ», три плитки, тизер 13 покрытий) → новая `<section class="what" id="prod">`: две половины бок о бок, кремовая гальваника / графитовый вакуум.
- Блоки «Оборудование и мощности» и «Лицензии и сертификаты» остаются ниже без изменений, в секции `.production#prod-blocks`.
- Старые кнопки под сертификатами («Запросить КП для тендера», «Каталог всех покрытий») убраны — дублируют «Расчёт по КД и ТЗ» и «Каталог покрытий» в новой секции.
- Якорь `#prod` сохранён — пункт меню «Что делаем» работает.

## Файлы
- `what.css` — стили секции (из final-emil, без глобальных сбросов; служебные переменные — на `.what`).
- `what.js` — механика: тон половины, образец, карточка строки, поворот к руке.
- Шрифты: Unbounded 300/500/700 уже в `fonts/google.css`, внешних подключений нет.

## Ссылки
- «Расчёт по КД и ТЗ» → `#contact`, `data-event="cta_calculate"`.
- «Каталог покрытий» → `coatings/`. «Плёнки и подложки» → `vakuum.html`.

## Факты
Все тексты и толщины — из каталога и документа клиента; новых утверждений нет. ГОСТ-обозначений (9.306-85) нет — по решению Артёма.

## Движение
Только в ответ на действие человека. Тон половины 360 мс, слой на образце 400 мс (потолок), карточка 100 мс, диск 220 мс, число 260 мс; возврат блика 800 мс. `prefers-reduced-motion` — статично. Строки — `<button aria-pressed>`, Tab выбирает образец.

## Проверка
1440 и 390 — скриншотами; hover по строкам; клавиатура; поиск по сайту (`tools/gen-search-index.py`); `#prod` из меню.
